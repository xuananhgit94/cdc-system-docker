{
  "name": "posts-source",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "plugin.name": "pgoutput",
    "database.hostname": "postgres",
    "database.port": "5432",
    "database.user": "cdc-data",
    "database.dbname": "cdc-data",
    "database.password": "xuananh",
    "topic.prefix": "cdc",
    "table.include.list": "public.posts",
    "transforms": "rename",
    "transforms.rename.type": "org.apache.kafka.connect.transforms.RegexRouter",
    "transforms.rename.regex": "(.*)",
    "transforms.rename.replacement": "cdc-stream-post"
  }
}